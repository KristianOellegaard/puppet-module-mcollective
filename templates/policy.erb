policy default deny
<% userpolicy.each do |user,settings| -%>
<%= settings["policy"] %>	<% if scope.lookupvar("mcollective::server::mc_security_provider") == 'ssl' %>cert=<% end %><%= user %>	<%= settings["actions"] ? settings["actions"].join(" ") : "*" %>	*	*
<% end -%>
